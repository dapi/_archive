<h1>Activate your account</h1>

<% form_for @user, :url => activate_path(@user.id), :html => { :method => :post } do |f| %>
	<%= f.error_messages %>
	<p class="submit">
		<%= f.submit "Activate" %>
	</p>
<% end %>