<h1>Изменить компанию</h1>

<% form_for([@company.category, @company]) do |f| %>
  <%= f.error_messages %>

  <%= render :partial => "form", :locals => { :f => f }%>

  <p>
    <%= f.submit "Изменить" %>
  </p>
<% end %>
