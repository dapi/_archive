<h1>Добавить компанию</h1>

<% form_for([:category, @company]) do |f| %>
  <%= f.error_messages %>

  <%= render :partial => "form", :locals => { :f => f }%>

  <p>
    <%= f.submit "Создать" %>
  </p>
<% end %>
